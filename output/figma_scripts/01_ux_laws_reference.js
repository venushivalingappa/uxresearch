// ===================================================
// UX Laws & Principles Reference
// Generated by UX Research Agent
// Paste into: Figma → Plugins → Development → Open Console
// ===================================================

async function run() {
  figma.currentPage.name = "UX Laws & Principles Reference";

  await figma.loadFontAsync({ family: 'Inter', style: 'Bold' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Regular' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Medium' });

  const lawsTitle = figma.createText();
  lawsTitle.fontName = { family: 'Inter', style: 'Bold' };
  lawsTitle.fontSize = 32; lawsTitle.x = 0; lawsTitle.y = 0;
  lawsTitle.characters = 'UX Laws & Principles Reference';
  figma.currentPage.appendChild(lawsTitle);

  // Law: Fitts' Law
  const card0 = figma.createFrame();
  card0.x = 0; card0.y = 80;
  card0.resize(440, 280);
  card0.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card0.cornerRadius = 8;
  card0.name = "Fitts' Law";
  figma.currentPage.appendChild(card0);

  // Priority bar
  const pbar0 = figma.createRectangle();
  pbar0.resize(440, 6); pbar0.x = 0; pbar0.y = 0;
  pbar0.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card0.appendChild(pbar0);

  const lTitle0 = figma.createText();
  lTitle0.fontName = { family: 'Inter', style: 'Bold' };
  lTitle0.fontSize = 16; lTitle0.x = 16; lTitle0.y = 18;
  lTitle0.characters = "Fitts' Law";
  card0.appendChild(lTitle0);

  const lDef0 = figma.createText();
  lDef0.fontName = { family: 'Inter', style: 'Regular' };
  lDef0.fontSize = 12; lDef0.x = 16; lDef0.y = 44;
  lDef0.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef0.textAutoResize = 'HEIGHT';
  lDef0.resize(408, 36);
  lDef0.characters = "The time to acquire a target is a function of the distance to and size of the target.";
  card0.appendChild(lDef0);

  const lDiv0 = figma.createRectangle();
  lDiv0.resize(408, 1); lDiv0.x = 16; lDiv0.y = 92;
  lDiv0.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card0.appendChild(lDiv0);

  const lApp0 = figma.createText();
  lApp0.fontName = { family: 'Inter', style: 'Regular' };
  lApp0.fontSize = 12; lApp0.x = 16; lApp0.y = 100;
  lApp0.textAutoResize = 'HEIGHT';
  lApp0.resize(408, 100);
  lApp0.characters = "During fintech onboarding, primary CTAs like 'Continue', 'Verify Identity', and 'Link Bank Account' must be large, thumb-reachable buttons placed at the bottom of the screen within the natural thumb a";
  card0.appendChild(lApp0);

  const lImpl0 = figma.createRectangle();
  lImpl0.resize(408, 44); lImpl0.x = 16; lImpl0.y = 220;
  lImpl0.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl0.cornerRadius = 4;
  card0.appendChild(lImpl0);

  const lImplText0 = figma.createText();
  lImplText0.fontName = { family: 'Inter', style: 'Medium' };
  lImplText0.fontSize = 11; lImplText0.x = 24; lImplText0.y = 228;
  lImplText0.textAutoResize = 'HEIGHT';
  lImplText0.resize(392, 32);
  lImplText0.characters = "\u2192 Make all primary onboarding action buttons at least 48x48dp, full-width where possible, and anchored to the bottom safe zone; never place cr";
  card0.appendChild(lImplText0);

  // Law: Hick's Law
  const card1 = figma.createFrame();
  card1.x = 480; card1.y = 80;
  card1.resize(440, 280);
  card1.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card1.cornerRadius = 8;
  card1.name = "Hick's Law";
  figma.currentPage.appendChild(card1);

  // Priority bar
  const pbar1 = figma.createRectangle();
  pbar1.resize(440, 6); pbar1.x = 0; pbar1.y = 0;
  pbar1.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card1.appendChild(pbar1);

  const lTitle1 = figma.createText();
  lTitle1.fontName = { family: 'Inter', style: 'Bold' };
  lTitle1.fontSize = 16; lTitle1.x = 16; lTitle1.y = 18;
  lTitle1.characters = "Hick's Law";
  card1.appendChild(lTitle1);

  const lDef1 = figma.createText();
  lDef1.fontName = { family: 'Inter', style: 'Regular' };
  lDef1.fontSize = 12; lDef1.x = 16; lDef1.y = 44;
  lDef1.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef1.textAutoResize = 'HEIGHT';
  lDef1.resize(408, 36);
  lDef1.characters = "The time to make a decision increases with the number and complexity of choices available.";
  card1.appendChild(lDef1);

  const lDiv1 = figma.createRectangle();
  lDiv1.resize(408, 1); lDiv1.x = 16; lDiv1.y = 92;
  lDiv1.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card1.appendChild(lDiv1);

  const lApp1 = figma.createText();
  lApp1.fontName = { family: 'Inter', style: 'Regular' };
  lApp1.fontSize = 12; lApp1.x = 16; lApp1.y = 100;
  lApp1.textAutoResize = 'HEIGHT';
  lApp1.resize(408, 100);
  lApp1.characters = "At the account setup stage, presenting users with multiple account types, investment options, or KYC verification methods simultaneously will paralyze new users who are already anxious about sharing f";
  card1.appendChild(lApp1);

  const lImpl1 = figma.createRectangle();
  lImpl1.resize(408, 44); lImpl1.x = 16; lImpl1.y = 220;
  lImpl1.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl1.cornerRadius = 4;
  card1.appendChild(lImpl1);

  const lImplText1 = figma.createText();
  lImplText1.fontName = { family: 'Inter', style: 'Medium' };
  lImplText1.fontSize = 11; lImplText1.x = 24; lImplText1.y = 228;
  lImplText1.textAutoResize = 'HEIGHT';
  lImplText1.resize(392, 32);
  lImplText1.characters = "\u2192 Limit each onboarding step to a single decision or input task; default to the most commonly used verification method (e.g., mobile OTP) and ";
  card1.appendChild(lImplText1);

  // Law: Miller's Law
  const card2 = figma.createFrame();
  card2.x = 960; card2.y = 80;
  card2.resize(440, 280);
  card2.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card2.cornerRadius = 8;
  card2.name = "Miller's Law";
  figma.currentPage.appendChild(card2);

  // Priority bar
  const pbar2 = figma.createRectangle();
  pbar2.resize(440, 6); pbar2.x = 0; pbar2.y = 0;
  pbar2.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card2.appendChild(pbar2);

  const lTitle2 = figma.createText();
  lTitle2.fontName = { family: 'Inter', style: 'Bold' };
  lTitle2.fontSize = 16; lTitle2.x = 16; lTitle2.y = 18;
  lTitle2.characters = "Miller's Law";
  card2.appendChild(lTitle2);

  const lDef2 = figma.createText();
  lDef2.fontName = { family: 'Inter', style: 'Regular' };
  lDef2.fontSize = 12; lDef2.x = 16; lDef2.y = 44;
  lDef2.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef2.textAutoResize = 'HEIGHT';
  lDef2.resize(408, 36);
  lDef2.characters = "The average person can hold approximately 7 (plus or minus 2) items in working memory at one time.";
  card2.appendChild(lDef2);

  const lDiv2 = figma.createRectangle();
  lDiv2.resize(408, 1); lDiv2.x = 16; lDiv2.y = 92;
  lDiv2.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card2.appendChild(lDiv2);

  const lApp2 = figma.createText();
  lApp2.fontName = { family: 'Inter', style: 'Regular' };
  lApp2.fontSize = 12; lApp2.x = 16; lApp2.y = 100;
  lApp2.textAutoResize = 'HEIGHT';
  lApp2.resize(408, 100);
  lApp2.characters = "Onboarding steps that require users to recall or enter long account numbers, IFSC codes, or policy reference numbers exceed working memory limits, especially when users must switch between apps to cop";
  card2.appendChild(lApp2);

  const lImpl2 = figma.createRectangle();
  lImpl2.resize(408, 44); lImpl2.x = 16; lImpl2.y = 220;
  lImpl2.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl2.cornerRadius = 4;
  card2.appendChild(lImpl2);

  const lImplText2 = figma.createText();
  lImplText2.fontName = { family: 'Inter', style: 'Medium' };
  lImplText2.fontSize = 11; lImplText2.x = 24; lImplText2.y = 228;
  lImplText2.textAutoResize = 'HEIGHT';
  lImplText2.resize(392, 32);
  lImplText2.characters = "\u2192 Chunk the onboarding flow into no more than 5 named stages shown in a progress bar, and pre-fill or auto-detect data (e.g., bank name from I";
  card2.appendChild(lImplText2);

  // Law: Jakob's Law
  const card3 = figma.createFrame();
  card3.x = 0; card3.y = 400;
  card3.resize(440, 280);
  card3.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card3.cornerRadius = 8;
  card3.name = "Jakob's Law";
  figma.currentPage.appendChild(card3);

  // Priority bar
  const pbar3 = figma.createRectangle();
  pbar3.resize(440, 6); pbar3.x = 0; pbar3.y = 0;
  pbar3.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card3.appendChild(pbar3);

  const lTitle3 = figma.createText();
  lTitle3.fontName = { family: 'Inter', style: 'Bold' };
  lTitle3.fontSize = 16; lTitle3.x = 16; lTitle3.y = 18;
  lTitle3.characters = "Jakob's Law";
  card3.appendChild(lTitle3);

  const lDef3 = figma.createText();
  lDef3.fontName = { family: 'Inter', style: 'Regular' };
  lDef3.fontSize = 12; lDef3.x = 16; lDef3.y = 44;
  lDef3.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef3.textAutoResize = 'HEIGHT';
  lDef3.resize(408, 36);
  lDef3.characters = "Users spend most of their time on other apps and expect your app to work the same way those apps do.";
  card3.appendChild(lDef3);

  const lDiv3 = figma.createRectangle();
  lDiv3.resize(408, 1); lDiv3.x = 16; lDiv3.y = 92;
  lDiv3.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card3.appendChild(lDiv3);

  const lApp3 = figma.createText();
  lApp3.fontName = { family: 'Inter', style: 'Regular' };
  lApp3.fontSize = 12; lApp3.x = 16; lApp3.y = 100;
  lApp3.textAutoResize = 'HEIGHT';
  lApp3.resize(408, 100);
  lApp3.characters = "Fintech users arrive with mental models from apps like Paytm, Groww, or Revolut, expecting OTP-based phone verification as the first onboarding step, not email registration. Deviating from the pattern";
  card3.appendChild(lApp3);

  const lImpl3 = figma.createRectangle();
  lImpl3.resize(408, 44); lImpl3.x = 16; lImpl3.y = 220;
  lImpl3.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl3.cornerRadius = 4;
  card3.appendChild(lImpl3);

  const lImplText3 = figma.createText();
  lImplText3.fontName = { family: 'Inter', style: 'Medium' };
  lImplText3.fontSize = 11; lImplText3.x = 24; lImplText3.y = 228;
  lImplText3.textAutoResize = 'HEIGHT';
  lImplText3.resize(392, 32);
  lImplText3.characters = "\u2192 Mirror the onboarding sequence convention of phone verification \u2192 OTP \u2192 minimal profile \u2192 KYC, and use standard platform components (native ";
  card3.appendChild(lImplText3);

  // Law: Law of Proximity
  const card4 = figma.createFrame();
  card4.x = 480; card4.y = 400;
  card4.resize(440, 280);
  card4.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card4.cornerRadius = 8;
  card4.name = "Law of Proximity";
  figma.currentPage.appendChild(card4);

  // Priority bar
  const pbar4 = figma.createRectangle();
  pbar4.resize(440, 6); pbar4.x = 0; pbar4.y = 0;
  pbar4.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.7, b: 0.2 } }];
  card4.appendChild(pbar4);

  const lTitle4 = figma.createText();
  lTitle4.fontName = { family: 'Inter', style: 'Bold' };
  lTitle4.fontSize = 16; lTitle4.x = 16; lTitle4.y = 18;
  lTitle4.characters = "Law of Proximity";
  card4.appendChild(lTitle4);

  const lDef4 = figma.createText();
  lDef4.fontName = { family: 'Inter', style: 'Regular' };
  lDef4.fontSize = 12; lDef4.x = 16; lDef4.y = 44;
  lDef4.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef4.textAutoResize = 'HEIGHT';
  lDef4.resize(408, 36);
  lDef4.characters = "Objects that are near each other are perceived as a group and assumed to be related.";
  card4.appendChild(lDef4);

  const lDiv4 = figma.createRectangle();
  lDiv4.resize(408, 1); lDiv4.x = 16; lDiv4.y = 92;
  lDiv4.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card4.appendChild(lDiv4);

  const lApp4 = figma.createText();
  lApp4.fontName = { family: 'Inter', style: 'Regular' };
  lApp4.fontSize = 12; lApp4.x = 16; lApp4.y = 100;
  lApp4.textAutoResize = 'HEIGHT';
  lApp4.resize(408, 100);
  lApp4.characters = "On the KYC document upload screen, placing the upload button, file size limit, and accepted formats far apart will make users uncertain about what the button applies to. Form fields for 'First Name' a";
  card4.appendChild(lApp4);

  const lImpl4 = figma.createRectangle();
  lImpl4.resize(408, 44); lImpl4.x = 16; lImpl4.y = 220;
  lImpl4.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl4.cornerRadius = 4;
  card4.appendChild(lImpl4);

  const lImplText4 = figma.createText();
  lImplText4.fontName = { family: 'Inter', style: 'Medium' };
  lImplText4.fontSize = 11; lImplText4.x = 24; lImplText4.y = 228;
  lImplText4.textAutoResize = 'HEIGHT';
  lImplText4.resize(392, 32);
  lImplText4.characters = "\u2192 Place all contextual help text, format hints, and security badges within 8px of the specific input field they relate to, not in a generic fo";
  card4.appendChild(lImplText4);

  // Law: Aesthetic-Usability Effect
  const card5 = figma.createFrame();
  card5.x = 960; card5.y = 400;
  card5.resize(440, 280);
  card5.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card5.cornerRadius = 8;
  card5.name = "Aesthetic-Usability Effect";
  figma.currentPage.appendChild(card5);

  // Priority bar
  const pbar5 = figma.createRectangle();
  pbar5.resize(440, 6); pbar5.x = 0; pbar5.y = 0;
  pbar5.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card5.appendChild(pbar5);

  const lTitle5 = figma.createText();
  lTitle5.fontName = { family: 'Inter', style: 'Bold' };
  lTitle5.fontSize = 16; lTitle5.x = 16; lTitle5.y = 18;
  lTitle5.characters = "Aesthetic-Usability Effect";
  card5.appendChild(lTitle5);

  const lDef5 = figma.createText();
  lDef5.fontName = { family: 'Inter', style: 'Regular' };
  lDef5.fontSize = 12; lDef5.x = 16; lDef5.y = 44;
  lDef5.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef5.textAutoResize = 'HEIGHT';
  lDef5.resize(408, 36);
  lDef5.characters = "Users perceive aesthetically pleasing designs as more usable and trustworthy, even when functionality is identical.";
  card5.appendChild(lDef5);

  const lDiv5 = figma.createRectangle();
  lDiv5.resize(408, 1); lDiv5.x = 16; lDiv5.y = 92;
  lDiv5.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card5.appendChild(lDiv5);

  const lApp5 = figma.createText();
  lApp5.fontName = { family: 'Inter', style: 'Regular' };
  lApp5.fontSize = 12; lApp5.x = 16; lApp5.y = 100;
  lApp5.textAutoResize = 'HEIGHT';
  lApp5.resize(408, 100);
  lApp5.characters = "In fintech onboarding, where users are asked to submit sensitive documents and link bank accounts, a polished visual design directly increases perceived trustworthiness and willingness to share data. ";
  card5.appendChild(lApp5);

  const lImpl5 = figma.createRectangle();
  lImpl5.resize(408, 44); lImpl5.x = 16; lImpl5.y = 220;
  lImpl5.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl5.cornerRadius = 4;
  card5.appendChild(lImpl5);

  const lImplText5 = figma.createText();
  lImplText5.fontName = { family: 'Inter', style: 'Medium' };
  lImplText5.fontSize = 11; lImplText5.x = 24; lImplText5.y = 228;
  lImplText5.textAutoResize = 'HEIGHT';
  lImplText5.resize(392, 32);
  lImplText5.characters = "\u2192 Invest in pixel-perfect visual design for the KYC and bank-linking screens specifically \u2014 these are the highest trust-risk moments \u2014 using c";
  card5.appendChild(lImplText5);

  // Law: Peak-End Rule
  const card6 = figma.createFrame();
  card6.x = 0; card6.y = 720;
  card6.resize(440, 280);
  card6.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card6.cornerRadius = 8;
  card6.name = "Peak-End Rule";
  figma.currentPage.appendChild(card6);

  // Priority bar
  const pbar6 = figma.createRectangle();
  pbar6.resize(440, 6); pbar6.x = 0; pbar6.y = 0;
  pbar6.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card6.appendChild(pbar6);

  const lTitle6 = figma.createText();
  lTitle6.fontName = { family: 'Inter', style: 'Bold' };
  lTitle6.fontSize = 16; lTitle6.x = 16; lTitle6.y = 18;
  lTitle6.characters = "Peak-End Rule";
  card6.appendChild(lTitle6);

  const lDef6 = figma.createText();
  lDef6.fontName = { family: 'Inter', style: 'Regular' };
  lDef6.fontSize = 12; lDef6.x = 16; lDef6.y = 44;
  lDef6.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef6.textAutoResize = 'HEIGHT';
  lDef6.resize(408, 36);
  lDef6.characters = "People judge an experience based on how they felt at its most intense moment and at its end, not the average of every st";
  card6.appendChild(lDef6);

  const lDiv6 = figma.createRectangle();
  lDiv6.resize(408, 1); lDiv6.x = 16; lDiv6.y = 92;
  lDiv6.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card6.appendChild(lDiv6);

  const lApp6 = figma.createText();
  lApp6.fontName = { family: 'Inter', style: 'Regular' };
  lApp6.fontSize = 12; lApp6.x = 16; lApp6.y = 100;
  lApp6.textAutoResize = 'HEIGHT';
  lApp6.resize(408, 100);
  lApp6.characters = "The 'peak' in fintech onboarding is likely the KYC document submission or bank account linking \u2014 high anxiety, high effort moments that must be actively softened with reassurance, progress feedback, a";
  card6.appendChild(lApp6);

  const lImpl6 = figma.createRectangle();
  lImpl6.resize(408, 44); lImpl6.x = 16; lImpl6.y = 220;
  lImpl6.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl6.cornerRadius = 4;
  card6.appendChild(lImpl6);

  const lImplText6 = figma.createText();
  lImplText6.fontName = { family: 'Inter', style: 'Medium' };
  lImplText6.fontSize = 11; lImplText6.x = 24; lImplText6.y = 228;
  lImplText6.textAutoResize = 'HEIGHT';
  lImplText6.resize(392, 32);
  lImplText6.characters = "\u2192 Design a dedicated, animated success screen at onboarding completion \u2014 showing the user's name, account status, and a clear first action to ";
  card6.appendChild(lImplText6);

  // Law: Doherty Threshold
  const card7 = figma.createFrame();
  card7.x = 480; card7.y = 720;
  card7.resize(440, 280);
  card7.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card7.cornerRadius = 8;
  card7.name = "Doherty Threshold";
  figma.currentPage.appendChild(card7);

  // Priority bar
  const pbar7 = figma.createRectangle();
  pbar7.resize(440, 6); pbar7.x = 0; pbar7.y = 0;
  pbar7.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.7, b: 0.2 } }];
  card7.appendChild(pbar7);

  const lTitle7 = figma.createText();
  lTitle7.fontName = { family: 'Inter', style: 'Bold' };
  lTitle7.fontSize = 16; lTitle7.x = 16; lTitle7.y = 18;
  lTitle7.characters = "Doherty Threshold";
  card7.appendChild(lTitle7);

  const lDef7 = figma.createText();
  lDef7.fontName = { family: 'Inter', style: 'Regular' };
  lDef7.fontSize = 12; lDef7.x = 16; lDef7.y = 44;
  lDef7.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef7.textAutoResize = 'HEIGHT';
  lDef7.resize(408, 36);
  lDef7.characters = "Productivity and engagement soar when a computer and its users interact at a pace of under 400ms, eliminating any need f";
  card7.appendChild(lDef7);

  const lDiv7 = figma.createRectangle();
  lDiv7.resize(408, 1); lDiv7.x = 16; lDiv7.y = 92;
  lDiv7.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card7.appendChild(lDiv7);

  const lApp7 = figma.createText();
  lApp7.fontName = { family: 'Inter', style: 'Regular' };
  lApp7.fontSize = 12; lApp7.x = 16; lApp7.y = 100;
  lApp7.textAutoResize = 'HEIGHT';
  lApp7.resize(408, 100);
  lApp7.characters = "During onboarding, if OTP verification, bank account validation, or KYC document processing takes more than 400ms without visual feedback, users interpret the silence as an error and either tap again ";
  card7.appendChild(lApp7);

  const lImpl7 = figma.createRectangle();
  lImpl7.resize(408, 44); lImpl7.x = 16; lImpl7.y = 220;
  lImpl7.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl7.cornerRadius = 4;
  card7.appendChild(lImpl7);

  const lImplText7 = figma.createText();
  lImplText7.fontName = { family: 'Inter', style: 'Medium' };
  lImplText7.fontSize = 11; lImplText7.x = 24; lImplText7.y = 228;
  lImplText7.textAutoResize = 'HEIGHT';
  lImplText7.resize(392, 32);
  lImplText7.characters = "\u2192 Instrument all onboarding API calls to target sub-400ms response times, and for calls that inherently take longer (e.g., KYC processing), im";
  card7.appendChild(lImplText7);

  // Law: Postel's Law
  const card8 = figma.createFrame();
  card8.x = 960; card8.y = 720;
  card8.resize(440, 280);
  card8.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card8.cornerRadius = 8;
  card8.name = "Postel's Law";
  figma.currentPage.appendChild(card8);

  // Priority bar
  const pbar8 = figma.createRectangle();
  pbar8.resize(440, 6); pbar8.x = 0; pbar8.y = 0;
  pbar8.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.7, b: 0.2 } }];
  card8.appendChild(pbar8);

  const lTitle8 = figma.createText();
  lTitle8.fontName = { family: 'Inter', style: 'Bold' };
  lTitle8.fontSize = 16; lTitle8.x = 16; lTitle8.y = 18;
  lTitle8.characters = "Postel's Law";
  card8.appendChild(lTitle8);

  const lDef8 = figma.createText();
  lDef8.fontName = { family: 'Inter', style: 'Regular' };
  lDef8.fontSize = 12; lDef8.x = 16; lDef8.y = 44;
  lDef8.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef8.textAutoResize = 'HEIGHT';
  lDef8.resize(408, 36);
  lDef8.characters = "Be liberal in what you accept from users, and conservative in what you output.";
  card8.appendChild(lDef8);

  const lDiv8 = figma.createRectangle();
  lDiv8.resize(408, 1); lDiv8.x = 16; lDiv8.y = 92;
  lDiv8.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card8.appendChild(lDiv8);

  const lApp8 = figma.createText();
  lApp8.fontName = { family: 'Inter', style: 'Regular' };
  lApp8.fontSize = 12; lApp8.x = 16; lApp8.y = 100;
  lApp8.textAutoResize = 'HEIGHT';
  lApp8.resize(408, 100);
  lApp8.characters = "During onboarding form entry, if a user types their phone number with spaces, dashes, or a country code prefix, the app should silently normalize it rather than throwing a validation error that breaks";
  card8.appendChild(lApp8);

  const lImpl8 = figma.createRectangle();
  lImpl8.resize(408, 44); lImpl8.x = 16; lImpl8.y = 220;
  lImpl8.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl8.cornerRadius = 4;
  card8.appendChild(lImpl8);

  const lImplText8 = figma.createText();
  lImplText8.fontName = { family: 'Inter', style: 'Medium' };
  lImplText8.fontSize = 11; lImplText8.x = 24; lImplText8.y = 228;
  lImplText8.textAutoResize = 'HEIGHT';
  lImplText8.resize(392, 32);
  lImplText8.characters = "\u2192 Build input normalization logic for all onboarding fields \u2014 phone numbers, names, dates, account numbers \u2014 so the app strips formatting nois";
  card8.appendChild(lImplText8);

  // Law: Progressive Disclosure
  const card9 = figma.createFrame();
  card9.x = 0; card9.y = 1040;
  card9.resize(440, 280);
  card9.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card9.cornerRadius = 8;
  card9.name = "Progressive Disclosure";
  figma.currentPage.appendChild(card9);

  // Priority bar
  const pbar9 = figma.createRectangle();
  pbar9.resize(440, 6); pbar9.x = 0; pbar9.y = 0;
  pbar9.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card9.appendChild(pbar9);

  const lTitle9 = figma.createText();
  lTitle9.fontName = { family: 'Inter', style: 'Bold' };
  lTitle9.fontSize = 16; lTitle9.x = 16; lTitle9.y = 18;
  lTitle9.characters = "Progressive Disclosure";
  card9.appendChild(lTitle9);

  const lDef9 = figma.createText();
  lDef9.fontName = { family: 'Inter', style: 'Regular' };
  lDef9.fontSize = 12; lDef9.x = 16; lDef9.y = 44;
  lDef9.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef9.textAutoResize = 'HEIGHT';
  lDef9.resize(408, 36);
  lDef9.characters = "Present only the information necessary for the current task, revealing additional detail only as users need it.";
  card9.appendChild(lDef9);

  const lDiv9 = figma.createRectangle();
  lDiv9.resize(408, 1); lDiv9.x = 16; lDiv9.y = 92;
  lDiv9.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card9.appendChild(lDiv9);

  const lApp9 = figma.createText();
  lApp9.fontName = { family: 'Inter', style: 'Regular' };
  lApp9.fontSize = 12; lApp9.x = 16; lApp9.y = 100;
  lApp9.textAutoResize = 'HEIGHT';
  lApp9.resize(408, 100);
  lApp9.characters = "Asking a new user to complete full KYC, set notification preferences, choose a spending category, and set a PIN all before seeing the app's core value will cause drop-off. The onboarding flow should u";
  card9.appendChild(lApp9);

  const lImpl9 = figma.createRectangle();
  lImpl9.resize(408, 44); lImpl9.x = 16; lImpl9.y = 220;
  lImpl9.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl9.cornerRadius = 4;
  card9.appendChild(lImpl9);

  const lImplText9 = figma.createText();
  lImplText9.fontName = { family: 'Inter', style: 'Medium' };
  lImplText9.fontSize = 11; lImplText9.x = 24; lImplText9.y = 228;
  lImplText9.textAutoResize = 'HEIGHT';
  lImplText9.resize(392, 32);
  lImplText9.characters = "\u2192 Scope the onboarding critical path to the minimum fields legally required for account activation, and schedule all secondary personalisation";
  card9.appendChild(lImplText9);

  // Law: Tesler's Law (Conservation of Complexity)
  const card10 = figma.createFrame();
  card10.x = 480; card10.y = 1040;
  card10.resize(440, 280);
  card10.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card10.cornerRadius = 8;
  card10.name = "Tesler's Law (Conservation of Complexity)";
  figma.currentPage.appendChild(card10);

  // Priority bar
  const pbar10 = figma.createRectangle();
  pbar10.resize(440, 6); pbar10.x = 0; pbar10.y = 0;
  pbar10.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.3, b: 0.3 } }];
  card10.appendChild(pbar10);

  const lTitle10 = figma.createText();
  lTitle10.fontName = { family: 'Inter', style: 'Bold' };
  lTitle10.fontSize = 16; lTitle10.x = 16; lTitle10.y = 18;
  lTitle10.characters = "Tesler's Law (Conservation of Complexity)";
  card10.appendChild(lTitle10);

  const lDef10 = figma.createText();
  lDef10.fontName = { family: 'Inter', style: 'Regular' };
  lDef10.fontSize = 12; lDef10.x = 16; lDef10.y = 44;
  lDef10.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef10.textAutoResize = 'HEIGHT';
  lDef10.resize(408, 36);
  lDef10.characters = "Every application has an inherent amount of complexity that cannot be removed \u2014 it can only be shifted between the syste";
  card10.appendChild(lDef10);

  const lDiv10 = figma.createRectangle();
  lDiv10.resize(408, 1); lDiv10.x = 16; lDiv10.y = 92;
  lDiv10.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card10.appendChild(lDiv10);

  const lApp10 = figma.createText();
  lApp10.fontName = { family: 'Inter', style: 'Regular' };
  lApp10.fontSize = 12; lApp10.x = 16; lApp10.y = 100;
  lApp10.textAutoResize = 'HEIGHT';
  lApp10.resize(408, 100);
  lApp10.characters = "Fintech onboarding carries unavoidable regulatory complexity: KYC laws require identity verification, AML rules require source-of-funds disclosures, and data privacy laws require consent capture. The ";
  card10.appendChild(lApp10);

  const lImpl10 = figma.createRectangle();
  lImpl10.resize(408, 44); lImpl10.x = 16; lImpl10.y = 220;
  lImpl10.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl10.cornerRadius = 4;
  card10.appendChild(lImpl10);

  const lImplText10 = figma.createText();
  lImplText10.fontName = { family: 'Inter', style: 'Medium' };
  lImplText10.fontSize = 11; lImplText10.x = 24; lImplText10.y = 228;
  lImplText10.textAutoResize = 'HEIGHT';
  lImplText10.resize(392, 32);
  lImplText10.characters = "\u2192 Prioritize backend integrations with identity verification APIs (e.g., DigiLocker, Aadhaar eKYC, Plaid) that allow the system to absorb the ";
  card10.appendChild(lImplText10);

  // Law: Von Restorff Effect
  const card11 = figma.createFrame();
  card11.x = 960; card11.y = 1040;
  card11.resize(440, 280);
  card11.fills = [{ type: 'SOLID', color: { r: 0.97, g: 0.97, b: 0.97 } }];
  card11.cornerRadius = 8;
  card11.name = "Von Restorff Effect";
  figma.currentPage.appendChild(card11);

  // Priority bar
  const pbar11 = figma.createRectangle();
  pbar11.resize(440, 6); pbar11.x = 0; pbar11.y = 0;
  pbar11.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.7, b: 0.2 } }];
  card11.appendChild(pbar11);

  const lTitle11 = figma.createText();
  lTitle11.fontName = { family: 'Inter', style: 'Bold' };
  lTitle11.fontSize = 16; lTitle11.x = 16; lTitle11.y = 18;
  lTitle11.characters = "Von Restorff Effect";
  card11.appendChild(lTitle11);

  const lDef11 = figma.createText();
  lDef11.fontName = { family: 'Inter', style: 'Regular' };
  lDef11.fontSize = 12; lDef11.x = 16; lDef11.y = 44;
  lDef11.fills = [{ type: 'SOLID', color: { r: 0.4, g: 0.4, b: 0.4 } }];
  lDef11.textAutoResize = 'HEIGHT';
  lDef11.resize(408, 36);
  lDef11.characters = "When multiple similar objects are present, the one that differs from the rest is most likely to be remembered and notice";
  card11.appendChild(lDef11);

  const lDiv11 = figma.createRectangle();
  lDiv11.resize(408, 1); lDiv11.x = 16; lDiv11.y = 92;
  lDiv11.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.85, b: 0.85 } }];
  card11.appendChild(lDiv11);

  const lApp11 = figma.createText();
  lApp11.fontName = { family: 'Inter', style: 'Regular' };
  lApp11.fontSize = 12; lApp11.x = 16; lApp11.y = 100;
  lApp11.textAutoResize = 'HEIGHT';
  lApp11.resize(408, 100);
  lApp11.characters = "On a screen listing onboarding steps or required documents, users will only notice and remember items that are visually differentiated \u2014 making the 'currently active' step or a mandatory-but-overlooke";
  card11.appendChild(lApp11);

  const lImpl11 = figma.createRectangle();
  lImpl11.resize(408, 44); lImpl11.x = 16; lImpl11.y = 220;
  lImpl11.fills = [{ type: 'SOLID', color: { r: 0.95, g: 1.0, b: 0.95 } }];
  lImpl11.cornerRadius = 4;
  card11.appendChild(lImpl11);

  const lImplText11 = figma.createText();
  lImplText11.fontName = { family: 'Inter', style: 'Medium' };
  lImplText11.fontSize = 11; lImplText11.x = 24; lImplText11.y = 228;
  lImplText11.textAutoResize = 'HEIGHT';
  lImplText11.resize(392, 32);
  lImplText11.characters = "\u2192 Apply a single high-contrast accent color exclusively to the primary CTA on each onboarding screen, ensure 'Skip' or 'Do Later' options are ";
  card11.appendChild(lImplText11);

  figma.viewport.scrollAndZoomIntoView(figma.currentPage.children);
  figma.notify("UX Laws & Principles Reference created!");
  figma.closePlugin();
}

run().catch(err => {
  figma.notify('Error: ' + err.message, { error: true });
  figma.closePlugin();
});