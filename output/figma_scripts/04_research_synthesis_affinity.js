// ===================================================
// Research Synthesis — Affinity Map
// Generated by UX Research Agent
// Paste into: Figma → Plugins → Development → Open Console
// ===================================================

async function run() {
  figma.currentPage.name = "Research Synthesis \u2014 Affinity Map";

  await figma.loadFontAsync({ family: 'Inter', style: 'Bold' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Regular' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Medium' });

  // Cluster 1: Speed & Friction Tolerance
  const clusterHdr0 = figma.createRectangle();
  clusterHdr0.x = 0; clusterHdr0.y = 0;
  clusterHdr0.resize(460, 60);
  clusterHdr0.fills = [{ type: 'SOLID', color: { r: 0.12, g: 0.12, b: 0.12 } }];
  clusterHdr0.cornerRadius = 8;
  figma.currentPage.appendChild(clusterHdr0);

  const clusterTitle0 = figma.createText();
  clusterTitle0.fontName = { family: 'Inter', style: 'Bold' };
  clusterTitle0.fontSize = 18; clusterTitle0.x = 16; clusterTitle0.y = 18;
  clusterTitle0.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  clusterTitle0.characters = "Speed & Friction Tolerance";
  figma.currentPage.appendChild(clusterTitle0);

  const obs0_0 = figma.createSticky();
  obs0_0.x = 16; obs0_0.y = 80;
  obs0_0.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.4, a: 1 } }];
  obs0_0.text.characters = "From Marcus Chen: He wants to skip optional steps and get to investment features as fast as possible, indicating a strong preference for progressive disclosure over front-loaded data collection.";
  figma.currentPage.appendChild(obs0_0);

  const obs0_1 = figma.createSticky();
  obs0_1.x = 16; obs0_1.y = 280;
  obs0_1.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.4, a: 1 } }];
  obs0_1.text.characters = "From Marcus Chen: He is frustrated when apps do not support biometric login or autofill from the start, showing that power users expect zero-friction authentication from day one.";
  figma.currentPage.appendChild(obs0_1);

  const obs0_2 = figma.createSticky();
  obs0_2.x = 16; obs0_2.y = 480;
  obs0_2.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.4, a: 1 } }];
  obs0_2.text.characters = "From UX Laws: Hick's Law requires limiting each onboarding step to a single decision or input task, with the most common verification method defaulted and alternatives offered only as fallback.";
  figma.currentPage.appendChild(obs0_2);

  const obs0_3 = figma.createSticky();
  obs0_3.x = 16; obs0_3.y = 680;
  obs0_3.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.4, a: 1 } }];
  obs0_3.text.characters = "From Design Thinking Ideate phase: A Value-First Deferred Onboarding concept emerged as the most generative radical idea, suggesting users should experience app value before being required to complete full registration.";
  figma.currentPage.appendChild(obs0_3);

  const insight0 = figma.createSticky();
  insight0.x = 16; insight0.y = 900;
  insight0.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.75, b: 1.0, a: 1 } }];
  insight0.text.characters = "\ud83d\udca1 Experienced users actively resist onboarding friction and respond best when the flow defaults intelligently, defers optional steps, and surfaces value before demanding full commitment.";
  figma.currentPage.appendChild(insight0);

  // Cluster 2: Trust, Legibility & Visual Confidence
  const clusterHdr1 = figma.createRectangle();
  clusterHdr1.x = 500; clusterHdr1.y = 0;
  clusterHdr1.resize(460, 60);
  clusterHdr1.fills = [{ type: 'SOLID', color: { r: 0.12, g: 0.12, b: 0.12 } }];
  clusterHdr1.cornerRadius = 8;
  figma.currentPage.appendChild(clusterHdr1);

  const clusterTitle1 = figma.createText();
  clusterTitle1.fontName = { family: 'Inter', style: 'Bold' };
  clusterTitle1.fontSize = 18; clusterTitle1.x = 516; clusterTitle1.y = 18;
  clusterTitle1.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  clusterTitle1.characters = "Trust, Legibility & Visual Confidence";
  figma.currentPage.appendChild(clusterTitle1);

  const obs1_0 = figma.createSticky();
  obs1_0.x = 516; obs1_0.y = 80;
  obs1_0.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.85, a: 1 } }];
  obs1_0.text.characters = "From Diane Kowalski: Financial jargon like APY, FDIC-insured, or routing number appearing without explanation causes anxiety and distrust during setup.";
  figma.currentPage.appendChild(obs1_0);

  const obs1_1 = figma.createSticky();
  obs1_1.x = 516; obs1_1.y = 280;
  obs1_1.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.85, a: 1 } }];
  obs1_1.text.characters = "From Diane Kowalski: Cluttered or untrustworthy-looking screens make her second-guess the app's legitimacy, linking visual design quality directly to perceived security.";
  figma.currentPage.appendChild(obs1_1);

  const obs1_2 = figma.createSticky();
  obs1_2.x = 516; obs1_2.y = 480;
  obs1_2.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.85, a: 1 } }];
  obs1_2.text.characters = "From UX Laws: Law of Proximity requires contextual help text, format hints, and security badges to be placed within 8px of the specific input field they relate to, not in a generic footer.";
  figma.currentPage.appendChild(obs1_2);

  const obs1_3 = figma.createSticky();
  obs1_3.x = 516; obs1_3.y = 680;
  obs1_3.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.85, a: 1 } }];
  obs1_3.text.characters = "From Design Thinking Define phase: The defining tension in this problem is not between simplicity and power but between legibility and trust.";
  figma.currentPage.appendChild(obs1_3);

  const insight1 = figma.createSticky();
  insight1.x = 516; insight1.y = 900;
  insight1.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.75, b: 1.0, a: 1 } }];
  insight1.text.characters = "\ud83d\udca1 Users interpret visual clarity and contextually placed explanations as signals of legitimacy, meaning design quality and inline guidance are not aesthetic choices but trust infrastructure.";
  figma.currentPage.appendChild(insight1);

  // Cluster 3: Accessibility & Physical Usability
  const clusterHdr2 = figma.createRectangle();
  clusterHdr2.x = 1000; clusterHdr2.y = 0;
  clusterHdr2.resize(460, 60);
  clusterHdr2.fills = [{ type: 'SOLID', color: { r: 0.12, g: 0.12, b: 0.12 } }];
  clusterHdr2.cornerRadius = 8;
  figma.currentPage.appendChild(clusterHdr2);

  const clusterTitle2 = figma.createText();
  clusterTitle2.fontName = { family: 'Inter', style: 'Bold' };
  clusterTitle2.fontSize = 18; clusterTitle2.x = 1016; clusterTitle2.y = 18;
  clusterTitle2.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  clusterTitle2.characters = "Accessibility & Physical Usability";
  figma.currentPage.appendChild(clusterTitle2);

  const obs2_0 = figma.createSticky();
  obs2_0.x = 1016; obs2_0.y = 80;
  obs2_0.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  obs2_0.text.characters = "From Roy Tanner: Small tap targets and dense form fields are hard to interact with accurately, making physical accessibility a functional barrier to completion.";
  figma.currentPage.appendChild(obs2_0);

  const obs2_1 = figma.createSticky();
  obs2_1.x = 1016; obs2_1.y = 280;
  obs2_1.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  obs2_1.text.characters = "From Roy Tanner: Session timeouts mid-onboarding when he steps away to find paper bank statements cause him to restart the entire process, creating a critical drop-off risk.";
  figma.currentPage.appendChild(obs2_1);

  const obs2_2 = figma.createSticky();
  obs2_2.x = 1016; obs2_2.y = 480;
  obs2_2.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  obs2_2.text.characters = "From Roy Tanner: SMS codes that expire quickly and force a full restart compound the trust erosion for users who need more time to gather information.";
  figma.currentPage.appendChild(obs2_2);

  const obs2_3 = figma.createSticky();
  obs2_3.x = 1016; obs2_3.y = 680;
  obs2_3.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  obs2_3.text.characters = "From UX Laws: Fitts' Law mandates all primary action buttons be at least 48x48dp, full-width, and anchored to the bottom safe zone to support accurate tapping on mobile.";
  figma.currentPage.appendChild(obs2_3);

  const insight2 = figma.createSticky();
  insight2.x = 1016; insight2.y = 900;
  insight2.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.75, b: 1.0, a: 1 } }];
  insight2.text.characters = "\ud83d\udca1 Older or less dexterous users face compounding failure loops when tap targets are too small and time-sensitive verification steps penalise the natural pace of real-world information gathering.";
  figma.currentPage.appendChild(insight2);

  // Cluster 4: Cognitive Load & Decision Architecture
  const clusterHdr3 = figma.createRectangle();
  clusterHdr3.x = 1500; clusterHdr3.y = 0;
  clusterHdr3.resize(460, 60);
  clusterHdr3.fills = [{ type: 'SOLID', color: { r: 0.12, g: 0.12, b: 0.12 } }];
  clusterHdr3.cornerRadius = 8;
  figma.currentPage.appendChild(clusterHdr3);

  const clusterTitle3 = figma.createText();
  clusterTitle3.fontName = { family: 'Inter', style: 'Bold' };
  clusterTitle3.fontSize = 18; clusterTitle3.x = 1516; clusterTitle3.y = 18;
  clusterTitle3.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  clusterTitle3.characters = "Cognitive Load & Decision Architecture";
  figma.currentPage.appendChild(clusterTitle3);

  const obs3_0 = figma.createSticky();
  obs3_0.x = 1516; obs3_0.y = 80;
  obs3_0.fills = [{ type: 'SOLID', color: { r: 0.75, g: 1.0, b: 0.75, a: 1 } }];
  obs3_0.text.characters = "From Diane Kowalski: Being asked to choose account types before understanding the differences makes her feel overwhelmed, confirming that decision overload early in the flow damages confidence.";
  figma.currentPage.appendChild(obs3_0);

  const obs3_1 = figma.createSticky();
  obs3_1.x = 1516; obs3_1.y = 280;
  obs3_1.fills = [{ type: 'SOLID', color: { r: 0.75, g: 1.0, b: 0.75, a: 1 } }];
  obs3_1.text.characters = "From Marcus Chen: He resents being shown tutorials for things he already understands, revealing that one-size-fits-all onboarding education creates friction for experienced fintech users.";
  figma.currentPage.appendChild(obs3_1);

  const obs3_2 = figma.createSticky();
  obs3_2.x = 1516; obs3_2.y = 480;
  obs3_2.fills = [{ type: 'SOLID', color: { r: 0.75, g: 1.0, b: 0.75, a: 1 } }];
  obs3_2.text.characters = "From UX Laws: Miller's Law recommends chunking the flow into no more than five named stages shown in a progress bar, and using auto-detection like IFSC prefix lookup to reduce recall load.";
  figma.currentPage.appendChild(obs3_2);

  const obs3_3 = figma.createSticky();
  obs3_3.x = 1516; obs3_3.y = 680;
  obs3_3.fills = [{ type: 'SOLID', color: { r: 0.75, g: 1.0, b: 0.75, a: 1 } }];
  obs3_3.text.characters = "From UX Laws: Jakob's Law supports mirroring the industry-standard sequence of phone verification, OTP, minimal profile, and KYC, using native platform components instead of custom ones.";
  figma.currentPage.appendChild(obs3_3);

  const insight3 = figma.createSticky();
  insight3.x = 1516; insight3.y = 900;
  insight3.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.75, b: 1.0, a: 1 } }];
  insight3.text.characters = "\ud83d\udca1 Cognitive overload manifests differently across experience levels, requiring the flow to sequence decisions in order of familiarity, chunk steps visibly, and lean on platform conventions to reduce mental effort for all users.";
  figma.currentPage.appendChild(insight3);

  // ─── How Might We Statements ───
  const hmwHdr = figma.createRectangle();
  hmwHdr.x = 2060; hmwHdr.y = 0;
  hmwHdr.resize(460, 60);
  hmwHdr.fills = [{ type: 'SOLID', color: { r: 0.2, g: 0.4, b: 0.9 } }];
  hmwHdr.cornerRadius = 8;
  figma.currentPage.appendChild(hmwHdr);

  const hmwTitle = figma.createText();
  hmwTitle.fontName = { family: 'Inter', style: 'Bold' };
  hmwTitle.fontSize = 18; hmwTitle.x = 2076; hmwTitle.y = 18;
  hmwTitle.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  hmwTitle.characters = 'How Might We...';
  figma.currentPage.appendChild(hmwTitle);

  const hmw0 = figma.createSticky();
  hmw0.x = 2076; hmw0.y = 80;
  hmw0.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  hmw0.text.characters = "How Might We design a tiered onboarding flow that lets power users skip optional steps while surfacing progressive guidance only for users who signal they need it";
  figma.currentPage.appendChild(hmw0);

  const hmw1 = figma.createSticky();
  hmw1.x = 2076; hmw1.y = 280;
  hmw1.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  hmw1.text.characters = "How Might We replace all financial jargon at the point of input with plain-language inline tooltips that appear within 8px of the relevant field without cluttering the screen";
  figma.currentPage.appendChild(hmw1);

  const hmw2 = figma.createSticky();
  hmw2.x = 2076; hmw2.y = 480;
  hmw2.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  hmw2.text.characters = "How Might We extend session persistence and OTP validity windows so users who need to retrieve physical documents are not penalised with forced restarts";
  figma.currentPage.appendChild(hmw2);

  const hmw3 = figma.createSticky();
  hmw3.x = 2076; hmw3.y = 680;
  hmw3.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  hmw3.text.characters = "How Might We apply Value-First Deferred Onboarding to let users explore core app features before completing KYC, reducing the perceived cost of starting the flow";
  figma.currentPage.appendChild(hmw3);

  const hmw4 = figma.createSticky();
  hmw4.x = 2076; hmw4.y = 880;
  hmw4.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.87, b: 1.0, a: 1 } }];
  hmw4.text.characters = "How Might We use auto-detection, native platform components, and smart defaults to reduce the number of manual inputs required across every onboarding stage";
  figma.currentPage.appendChild(hmw4);

  figma.viewport.scrollAndZoomIntoView(figma.currentPage.children);
  figma.notify("Research Synthesis \u2014 Affinity Map created!");
  figma.closePlugin();
}

run().catch(err => {
  figma.notify('Error: ' + err.message, { error: true });
  figma.closePlugin();
});