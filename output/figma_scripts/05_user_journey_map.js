// ===================================================
// User Journey Map
// Generated by UX Research Agent
// Paste into: Figma → Plugins → Development → Open Console
// ===================================================

async function run() {
  figma.currentPage.name = "User Journey Map";

  await figma.loadFontAsync({ family: 'Inter', style: 'Bold' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Regular' });
  await figma.loadFontAsync({ family: 'Inter', style: 'Medium' });

  const journeyTitle = figma.createText();
  journeyTitle.fontName = { family: 'Inter', style: 'Bold' };
  journeyTitle.fontSize = 28; journeyTitle.x = 120; journeyTitle.y = 20;
  journeyTitle.characters = "User Journey Map \u2014 Marcus Chen";
  figma.currentPage.appendChild(journeyTitle);

  const rowHdr0 = figma.createRectangle();
  rowHdr0.x = 0; rowHdr0.y = 100;
  rowHdr0.resize(110, 172);
  rowHdr0.fills = [{ type: 'SOLID', color: { r: 0.1, g: 0.1, b: 0.1 } }];
  figma.currentPage.appendChild(rowHdr0);

  const rowLabel0 = figma.createText();
  rowLabel0.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel0.fontSize = 10; rowLabel0.x = 8; rowLabel0.y = 180;
  rowLabel0.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel0.characters = "STAGE";
  figma.currentPage.appendChild(rowLabel0);

  const rowHdr1 = figma.createRectangle();
  rowHdr1.x = 0; rowHdr1.y = 280;
  rowHdr1.resize(110, 172);
  rowHdr1.fills = [{ type: 'SOLID', color: { r: 0.2, g: 0.4, b: 0.8 } }];
  figma.currentPage.appendChild(rowHdr1);

  const rowLabel1 = figma.createText();
  rowLabel1.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel1.fontSize = 10; rowLabel1.x = 8; rowLabel1.y = 360;
  rowLabel1.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel1.characters = "TOUCHPOINTS";
  figma.currentPage.appendChild(rowLabel1);

  const rowHdr2 = figma.createRectangle();
  rowHdr2.x = 0; rowHdr2.y = 460;
  rowHdr2.resize(110, 172);
  rowHdr2.fills = [{ type: 'SOLID', color: { r: 0.3, g: 0.3, b: 0.3 } }];
  figma.currentPage.appendChild(rowHdr2);

  const rowLabel2 = figma.createText();
  rowLabel2.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel2.fontSize = 10; rowLabel2.x = 8; rowLabel2.y = 540;
  rowLabel2.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel2.characters = "USER ACTIONS";
  figma.currentPage.appendChild(rowLabel2);

  const rowHdr3 = figma.createRectangle();
  rowHdr3.x = 0; rowHdr3.y = 640;
  rowHdr3.resize(110, 172);
  rowHdr3.fills = [{ type: 'SOLID', color: { r: 0.5, g: 0.3, b: 0.7 } }];
  figma.currentPage.appendChild(rowHdr3);

  const rowLabel3 = figma.createText();
  rowLabel3.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel3.fontSize = 10; rowLabel3.x = 8; rowLabel3.y = 720;
  rowLabel3.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel3.characters = "THOUGHTS";
  figma.currentPage.appendChild(rowLabel3);

  const rowHdr4 = figma.createRectangle();
  rowHdr4.x = 0; rowHdr4.y = 820;
  rowHdr4.resize(110, 172);
  rowHdr4.fills = [{ type: 'SOLID', color: { r: 0.1, g: 0.5, b: 0.3 } }];
  figma.currentPage.appendChild(rowHdr4);

  const rowLabel4 = figma.createText();
  rowLabel4.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel4.fontSize = 10; rowLabel4.x = 8; rowLabel4.y = 900;
  rowLabel4.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel4.characters = "EMOTIONS";
  figma.currentPage.appendChild(rowLabel4);

  const rowHdr5 = figma.createRectangle();
  rowHdr5.x = 0; rowHdr5.y = 1000;
  rowHdr5.resize(110, 172);
  rowHdr5.fills = [{ type: 'SOLID', color: { r: 0.7, g: 0.1, b: 0.1 } }];
  figma.currentPage.appendChild(rowHdr5);

  const rowLabel5 = figma.createText();
  rowLabel5.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel5.fontSize = 10; rowLabel5.x = 8; rowLabel5.y = 1080;
  rowLabel5.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel5.characters = "PAIN POINTS";
  figma.currentPage.appendChild(rowLabel5);

  const rowHdr6 = figma.createRectangle();
  rowHdr6.x = 0; rowHdr6.y = 1180;
  rowHdr6.resize(110, 172);
  rowHdr6.fills = [{ type: 'SOLID', color: { r: 0.1, g: 0.5, b: 0.5 } }];
  figma.currentPage.appendChild(rowHdr6);

  const rowLabel6 = figma.createText();
  rowLabel6.fontName = { family: 'Inter', style: 'Bold' };
  rowLabel6.fontSize = 10; rowLabel6.x = 8; rowLabel6.y = 1260;
  rowLabel6.fills = [{ type: 'SOLID', color: { r: 1, g: 1, b: 1 } }];
  rowLabel6.characters = "OPPORTUNITIES";
  figma.currentPage.appendChild(rowLabel6);

  const stageHdr0 = figma.createRectangle();
  stageHdr0.x = 120; stageHdr0.y = 100;
  stageHdr0.resize(272, 172);
  stageHdr0.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr0.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr0);

  const stageLabel0 = figma.createText();
  stageLabel0.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel0.fontSize = 15; stageLabel0.x = 132; stageLabel0.y = 120;
  stageLabel0.characters = "Awareness & Discovery";
  figma.currentPage.appendChild(stageLabel0);

  const tp0 = figma.createSticky();
  tp0.x = 120; tp0.y = 280;
  tp0.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp0.text.characters = "Twitter/X tech finance thread; App Store listing";
  figma.currentPage.appendChild(tp0);

  const act0 = figma.createSticky();
  act0.x = 120; act0.y = 460;
  act0.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act0.text.characters = "\u2022 Sees a recommendation for the app in a developer-focused finance thread\n\u2022 Reads the App Store description and scans feature highlights\n\u2022 Quickly checks ratings and skims a handful of reviews for red flags";
  figma.currentPage.appendChild(act0);

  const th0 = figma.createSticky();
  th0.x = 120; th0.y = 640;
  th0.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th0.text.characters = "\"Looks promising \u2014 the feature set is exactly what I want. Let me just hope the onboarding isn't a 20-step nightmare like the last one I tried.\"";
  figma.currentPage.appendChild(th0);

  const em0 = figma.createSticky();
  em0.x = 120; em0.y = 820;
  em0.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em0.text.characters = "\ud83d\ude0a curious cautiously optimistic (score: 1)";
  figma.currentPage.appendChild(em0);

  const pp0 = figma.createSticky();
  pp0.x = 120; pp0.y = 1000;
  pp0.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp0.text.characters = "\u26a0 App Store screenshots don't clearly show whether advanced investment features are accessible early\n\u26a0 Reviews mention a lengthy verification process but don't specify whether it can be deferred";
  figma.currentPage.appendChild(pp0);

  const op0 = figma.createSticky();
  op0.x = 120; op0.y = 1180;
  op0.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op0.text.characters = "\ud83d\udca1 How Might We apply Value-First Deferred Onboarding to let users explore core app features before completing KYC, reducing the perceived cost of starting the flow";
  figma.currentPage.appendChild(op0);

  const stageHdr1 = figma.createRectangle();
  stageHdr1.x = 400; stageHdr1.y = 100;
  stageHdr1.resize(272, 172);
  stageHdr1.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr1.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr1);

  const stageLabel1 = figma.createText();
  stageLabel1.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel1.fontSize = 15; stageLabel1.x = 412; stageLabel1.y = 120;
  stageLabel1.characters = "Download & Initial Launch";
  figma.currentPage.appendChild(stageLabel1);

  const tp1 = figma.createSticky();
  tp1.x = 400; tp1.y = 280;
  tp1.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp1.text.characters = "App Store download; App splash screen";
  figma.currentPage.appendChild(tp1);

  const act1 = figma.createSticky();
  act1.x = 400; act1.y = 460;
  act1.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act1.text.characters = "\u2022 Downloads the app and launches it immediately\n\u2022 Taps through the splash screen and branding intro\n\u2022 Grants Face ID and notification permissions when prompted natively";
  figma.currentPage.appendChild(act1);

  const th1 = figma.createSticky();
  th1.x = 400; th1.y = 640;
  th1.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th1.text.characters = "\"Good \u2014 it asked for Face ID right away using the native iOS prompt. That's a green flag. Let's see how bad the signup actually is.\"";
  figma.currentPage.appendChild(th1);

  const em1 = figma.createSticky();
  em1.x = 400; em1.y = 820;
  em1.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em1.text.characters = "\ud83d\ude10 impatient mildly relieved (score: 0)";
  figma.currentPage.appendChild(em1);

  const pp1 = figma.createSticky();
  pp1.x = 400; pp1.y = 1000;
  pp1.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp1.text.characters = "\u26a0 Branding animation on the splash screen feels like wasted time before he can interact\n\u26a0 Uncertainty about whether he can explore the app before committing to a full sign-up";
  figma.currentPage.appendChild(pp1);

  const op1 = figma.createSticky();
  op1.x = 400; op1.y = 1180;
  op1.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op1.text.characters = "\ud83d\udca1 How Might We apply Value-First Deferred Onboarding to let users explore core app features before completing KYC, reducing the perceived cost of starting the flow\n\ud83d\udca1 How Might We use auto-detection, native platform components, and smart defaults to reduce the number of manual inputs required across every onboarding stage";
  figma.currentPage.appendChild(op1);

  const stageHdr2 = figma.createRectangle();
  stageHdr2.x = 680; stageHdr2.y = 100;
  stageHdr2.resize(272, 172);
  stageHdr2.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr2.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr2);

  const stageLabel2 = figma.createText();
  stageLabel2.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel2.fontSize = 15; stageLabel2.x = 692; stageLabel2.y = 120;
  stageLabel2.characters = "Account Creation & Identity Verification";
  figma.currentPage.appendChild(stageLabel2);

  const tp2 = figma.createSticky();
  tp2.x = 680; tp2.y = 280;
  tp2.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp2.text.characters = "Sign-up screen; Email/phone input field";
  figma.currentPage.appendChild(tp2);

  const act2 = figma.createSticky();
  act2.x = 680; act2.y = 460;
  act2.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act2.text.characters = "\u2022 Selects 'Continue with Apple' to avoid manual data entry entirely\n\u2022 Confirms pre-filled name and email auto-populated from Apple ID\n\u2022 Receives and enters OTP which autofills from the iOS Messages app";
  figma.currentPage.appendChild(act2);

  const th2 = figma.createSticky();
  th2.x = 680; th2.y = 640;
  th2.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th2.text.characters = "\"The Apple Sign-In auto-filled everything \u2014 that saved me two minutes easily. But now it wants my government ID already? At least there's a 'do this later' option.\"";
  figma.currentPage.appendChild(th2);

  const em2 = figma.createSticky();
  em2.x = 680; em2.y = 820;
  em2.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em2.text.characters = "\ud83d\ude0a efficient mildly frustrated (score: 1)";
  figma.currentPage.appendChild(em2);

  const pp2 = figma.createSticky();
  pp2.x = 680; pp2.y = 1000;
  pp2.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp2.text.characters = "\u26a0 KYC prompt appearing before any value has been demonstrated feels premature\n\u26a0 No clear explanation of what features are locked versus accessible without completing KYC";
  figma.currentPage.appendChild(pp2);

  const op2 = figma.createSticky();
  op2.x = 680; op2.y = 1180;
  op2.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op2.text.characters = "\ud83d\udca1 How Might We use auto-detection, native platform components, and smart defaults to reduce the number of manual inputs required across every onboarding stage\n\ud83d\udca1 How Might We apply Value-First Deferred Onboarding to let users explore core app features before completing KYC, reducing the perceived cost of starting the flow";
  figma.currentPage.appendChild(op2);

  const stageHdr3 = figma.createRectangle();
  stageHdr3.x = 960; stageHdr3.y = 100;
  stageHdr3.resize(272, 172);
  stageHdr3.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr3.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr3);

  const stageLabel3 = figma.createText();
  stageLabel3.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel3.fontSize = 15; stageLabel3.x = 972; stageLabel3.y = 120;
  stageLabel3.characters = "Onboarding Flow & Profile Setup";
  figma.currentPage.appendChild(stageLabel3);

  const tp3 = figma.createSticky();
  tp3.x = 960; tp3.y = 280;
  tp3.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp3.text.characters = "Investor profile questionnaire; Risk tolerance screen";
  figma.currentPage.appendChild(tp3);

  const act3 = figma.createSticky();
  act3.x = 960; act3.y = 460;
  act3.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act3.text.characters = "\u2022 Sees a 'Power User' or 'Skip optional steps' prompt and immediately activates it\n\u2022 Completes only the required risk profile questions \u2014 3 instead of the full 8\n\u2022 Dismisses the tutorial overlay on the portfolio screen after a single tap";
  figma.currentPage.appendChild(act3);

  const th3 = figma.createSticky();
  th3.x = 960; th3.y = 640;
  th3.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th3.text.characters = "\"Finally \u2014 a flow that doesn't treat me like I've never used a phone before. Skipping those optional steps felt genuinely respectful of my time.\"";
  figma.currentPage.appendChild(th3);

  const em3 = figma.createSticky();
  em3.x = 960; em3.y = 820;
  em3.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em3.text.characters = "\ud83d\ude0a satisfied engaged (score: 2)";
  figma.currentPage.appendChild(em3);

  const pp3 = figma.createSticky();
  pp3.x = 960; pp3.y = 1000;
  pp3.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp3.text.characters = "\u26a0 Risk tolerance questions still use industry jargon like 'volatility tolerance' without inline clarification\n\u26a0 Progress indicator resets unexpectedly after skipping steps, causing brief confusion about completion state";
  figma.currentPage.appendChild(pp3);

  const op3 = figma.createSticky();
  op3.x = 960; op3.y = 1180;
  op3.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op3.text.characters = "\ud83d\udca1 How Might We design a tiered onboarding flow that lets power users skip optional steps while surfacing progressive guidance only for users who signal they need it\n\ud83d\udca1 How Might We replace all financial jargon at the point of input with plain-language inline tooltips that appear within 8px of the relevant field without cluttering the screen";
  figma.currentPage.appendChild(op3);

  const stageHdr4 = figma.createRectangle();
  stageHdr4.x = 1240; stageHdr4.y = 100;
  stageHdr4.resize(272, 172);
  stageHdr4.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr4.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr4);

  const stageLabel4 = figma.createText();
  stageLabel4.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel4.fontSize = 15; stageLabel4.x = 1252; stageLabel4.y = 120;
  stageLabel4.characters = "First Use & Feature Exploration";
  figma.currentPage.appendChild(stageLabel4);

  const tp4 = figma.createSticky();
  tp4.x = 1240; tp4.y = 280;
  tp4.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp4.text.characters = "Home dashboard; Investment discovery screen";
  figma.currentPage.appendChild(tp4);

  const act4 = figma.createSticky();
  act4.x = 1240; act4.y = 460;
  act4.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act4.text.characters = "\u2022 Lands on the dashboard and immediately navigates to the investment catalogue\n\u2022 Searches for specific ETFs and browses performance charts\n\u2022 Adds instruments to a watchlist to test the core loop before funding";
  figma.currentPage.appendChild(act4);

  const th4 = figma.createSticky();
  th4.x = 1240; th4.y = 640;
  th4.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th4.text.characters = "\"This is what I came for \u2014 the data is clean, the charts are responsive, and I haven't been interrupted by a single tutorial popup since I dismissed the first one.\"";
  figma.currentPage.appendChild(th4);

  const em4 = figma.createSticky();
  em4.x = 1240; em4.y = 820;
  em4.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em4.text.characters = "\ud83d\ude0a excited focused (score: 2)";
  figma.currentPage.appendChild(em4);

  const pp4 = figma.createSticky();
  pp4.x = 1240; pp4.y = 1000;
  pp4.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp4.text.characters = "\u26a0 Some premium analytics features are gated behind KYC completion with no preview or teaser available\n\u26a0 No persistent shortcut to resume KYC when he naturally decides he's ready";
  figma.currentPage.appendChild(pp4);

  const op4 = figma.createSticky();
  op4.x = 1240; op4.y = 1180;
  op4.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op4.text.characters = "\ud83d\udca1 How Might We apply Value-First Deferred Onboarding to let users explore core app features before completing KYC, reducing the perceived cost of starting the flow\n\ud83d\udca1 How Might We design a tiered onboarding flow that lets power users skip optional steps while surfacing progressive guidance only for users who signal they need it";
  figma.currentPage.appendChild(op4);

  const stageHdr5 = figma.createRectangle();
  stageHdr5.x = 1520; stageHdr5.y = 100;
  stageHdr5.resize(272, 172);
  stageHdr5.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr5.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr5);

  const stageLabel5 = figma.createText();
  stageLabel5.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel5.fontSize = 15; stageLabel5.x = 1532; stageLabel5.y = 120;
  stageLabel5.characters = "KYC Completion (Deferred Return)";
  figma.currentPage.appendChild(stageLabel5);

  const tp5 = figma.createSticky();
  tp5.x = 1520; tp5.y = 280;
  tp5.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp5.text.characters = "In-app KYC resume prompt; Document upload screen";
  figma.currentPage.appendChild(tp5);

  const act5 = figma.createSticky();
  act5.x = 1520; act5.y = 460;
  act5.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act5.text.characters = "\u2022 Taps the contextual banner prompting him to unlock full trading when he hits a feature gate\n\u2022 Resumes KYC from exactly where he left off \u2014 session was persisted\n\u2022 Uses phone camera to capture passport; document data is auto-extracted via OCR";
  figma.currentPage.appendChild(act5);

  const th5 = figma.createSticky();
  th5.x = 1520; th5.y = 640;
  th5.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th5.text.characters = "\"It remembered where I stopped and the OCR pulled my details straight from the passport photo \u2014 I barely had to type anything. This is how it should always work.\"";
  figma.currentPage.appendChild(th5);

  const em5 = figma.createSticky();
  em5.x = 1520; em5.y = 820;
  em5.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em5.text.characters = "\ud83d\ude0a impressed confident (score: 2)";
  figma.currentPage.appendChild(em5);

  const pp5 = figma.createSticky();
  pp5.x = 1520; pp5.y = 1000;
  pp5.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp5.text.characters = "\u26a0 Liveness check fails on first attempt due to lighting, with an error message that isn't specific enough to be actionable\n\u26a0 Provisional approval state is unclear \u2014 he's unsure which features are now unlocked versus pending full review";
  figma.currentPage.appendChild(pp5);

  const op5 = figma.createSticky();
  op5.x = 1520; op5.y = 1180;
  op5.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op5.text.characters = "\ud83d\udca1 How Might We extend session persistence and OTP validity windows so users who need to retrieve physical documents are not penalised with forced restarts\n\ud83d\udca1 How Might We use auto-detection, native platform components, and smart defaults to reduce the number of manual inputs required across every onboarding stage";
  figma.currentPage.appendChild(op5);

  const stageHdr6 = figma.createRectangle();
  stageHdr6.x = 1800; stageHdr6.y = 100;
  stageHdr6.resize(272, 172);
  stageHdr6.fills = [{ type: 'SOLID', color: { r: 0.9, g: 0.9, b: 0.9 } }];
  stageHdr6.cornerRadius = 6;
  figma.currentPage.appendChild(stageHdr6);

  const stageLabel6 = figma.createText();
  stageLabel6.fontName = { family: 'Inter', style: 'Bold' };
  stageLabel6.fontSize = 15; stageLabel6.x = 1812; stageLabel6.y = 120;
  stageLabel6.characters = "Regular Use & Advocacy";
  figma.currentPage.appendChild(stageLabel6);

  const tp6 = figma.createSticky();
  tp6.x = 1800; tp6.y = 280;
  tp6.fills = [{ type: 'SOLID', color: { r: 0.85, g: 0.92, b: 1.0, a: 1 } }];
  tp6.text.characters = "Daily app open via Face ID; Push notifications for portfolio events";
  figma.currentPage.appendChild(tp6);

  const act6 = figma.createSticky();
  act6.x = 1800; act6.y = 460;
  act6.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.95, b: 0.95, a: 1 } }];
  act6.text.characters = "\u2022 Opens the app daily using Face ID with zero friction from day one\n\u2022 Shares the app with two colleagues after a positive conversation about his portfolio returns\n\u2022 Leaves a 5-star App Store review specifically praising the skip-step onboarding";
  figma.currentPage.appendChild(act6);

  const th6 = figma.createSticky();
  th6.x = 1800; th6.y = 640;
  th6.fills = [{ type: 'SOLID', color: { r: 0.95, g: 0.87, b: 1.0, a: 1 } }];
  th6.text.characters = "\"I actually recommended this to someone \u2014 that's rare. The onboarding respected my intelligence and the core product delivered on the promise.\"";
  figma.currentPage.appendChild(th6);

  const em6 = figma.createSticky();
  em6.x = 1800; em6.y = 820;
  em6.fills = [{ type: 'SOLID', color: { r: 0.85, g: 1.0, b: 0.85, a: 1 } }];
  em6.text.characters = "\ud83d\ude0a loyal proud (score: 2)";
  figma.currentPage.appendChild(em6);

  const pp6 = figma.createSticky();
  pp6.x = 1800; pp6.y = 1000;
  pp6.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.75, b: 0.75, a: 1 } }];
  pp6.text.characters = "\u26a0 Referral flow requires the referred friend to complete full onboarding before Marcus receives credit, making the incentive feel distant\n\u26a0 Some advanced features introduced post-KYC have no contextual guidance, assuming discovery will happen organically";
  figma.currentPage.appendChild(pp6);

  const op6 = figma.createSticky();
  op6.x = 1800; op6.y = 1180;
  op6.fills = [{ type: 'SOLID', color: { r: 1.0, g: 0.95, b: 0.7, a: 1 } }];
  op6.text.characters = "\ud83d\udca1 How Might We design a tiered onboarding flow that lets power users skip optional steps while surfacing progressive guidance only for users who signal they need it";
  figma.currentPage.appendChild(op6);

  figma.viewport.scrollAndZoomIntoView(figma.currentPage.children);
  figma.notify("User Journey Map created!");
  figma.closePlugin();
}

run().catch(err => {
  figma.notify('Error: ' + err.message, { error: true });
  figma.closePlugin();
});